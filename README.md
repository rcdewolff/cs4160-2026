# CS4160 project

We have two versions of the project.
The code we used to submit originally is on the main branch.
We also attempted to tackle bonus assignment 3 (Storage that stays bounded), which was somewhat succesful, and simultaneously cleaned up the code a bit.
The code for the bonus assignment can be found on the `store-checkpoints-attempt` branch.
